# I-SELECT — سامانه هوشمند جذب و استخدام (مبتنی بر Excel + LLM محلی)

سامانه‌ای سبک و محلی (Local) برای **فیلتر، رتبه‌بندی و انتخاب کاندیداها** از روی فایل‌های Excel رزومه‌ها.
ورودی می‌تواند از **صوت کارفرما** (تشخیص گفتار مرورگر) یا **متن** باشد و سامانه به‌طور خودکار فرم را پر می‌کند، رزومه‌ها را فیلتر می‌کند، سپس با کمک **Ollama + Gemma** بهترین کاندیداها را **رتبه‌بندی** می‌کند و خروجی نهایی را به‌صورت **Excel** تحویل می‌دهد.

---

## قابلیت‌ها (Features)

* 🎙️ **پر کردن خودکار فرم با گفتار/متن** (fa-IR): سن، سابقه، شهر، جنسیت، نظام وظیفه، کلیدواژه‌های ضروری، تعداد نفرات برتر…
* 🔎 **فیلترهای قطعی محلی**: سن، سابقه، شهر، جنسیت، وضعیت خدمت (برای آقایان)، بدون استفاده از مدل.
* 🧩 **پیش‌فیلتر کلیدواژه‌ها**: فقط رزومه‌هایی که به نیازمندی‌های ضروری می‌خورند وارد مرحله LLM می‌شوند.
* 🤖 **رتبه‌بندی کاندیداها با LLM محلی** (Ollama + `gemma3:1b`) + **Fallback** داخلی هنگام خطای مدل.
* 🧠 **درک اعداد فارسی/عربی** و نگاشت **مقادیر تکی** (مثلاً «۲۸ سال») به **بازه‌ها** (مثلاً `25-32`) برای **سن** و **سابقه**.
* 📤 **خروجی Excel** با تب خلاصه تنظیمات و لیست نهایی کاندیداها + ستون «دلیل انتخاب».
* 📂 **انتخاب فایل رزومه** به‌صورت دستی یا **استفاده خودکار از فایل پیش‌فرض**
  `uploads/resumes_isaco_fa_10000.xlsx` (در صورت موجود بودن).
* 🧱 UI شیشه‌ای، راست‌به‌چپ، واکنش‌گرا، تک‌صفحه‌ای، با textarea هوشمند برای متن گفتاری.

---

## معماری کوتاه

* **Flask** برای وب‌اپ و رندر قالب (Jinja2)
* **Tailwind + Animate.css** برای UI
* **Ollama** برای اجرای مدل **Gemma** به‌صورت محلی
* **Pandas + openpyxl** برای پردازش Excel
* مسیرهای کلیدی:

  * `GET /` و `POST /` — فرم و پردازش کامل
  * `POST /nlp/parse` — تبدیل گفتار/متن به داده ساختاری (JSON)
  * `GET /download/<file>` — دریافت خروجی Excel

---

## ساختار پوشه‌ها

```
iSelect/
├─ app.py
├─ requirements.txt              # اختیاری؛ اگر ندارید، لیست زیر را نصب کنید
├─ templates/
│  └─ index.html
├─ uploads/
│  └─ resumes_isaco_fa_10000.xlsx   # (اختیاری) فایل پیش‌فرض رزومه‌ها
└─ outputs/
   └─ top_candidates_YYYYMMDD_HHMMSS.xlsx
```

> اگر پوشه‌ها وجود نداشتند، برنامه در اولین اجرا آن‌ها را می‌سازد.

---

## پیش‌نیازها

1. **Python 3.10+**
2. **Ollama** نصب و اجرا باشد:

   * دانلود/نصب: [https://ollama.com](https://ollama.com)
   * اجرای سرویس: معمولاً با نصب، سرویس خودش اجرا می‌شود. اگر لازم بود:

     ```bash
     ollama serve
     ```
   * دریافت مدل Gemma (نسخه سبک):

     ```bash
     ollama pull gemma3:1b
     ```
3. کتابخانه‌های پایتون:

   * Flask, pandas, openpyxl, difflib (در استاندارد), ollama (Python client)

**نصب پکیج‌ها** (Windows PowerShell):

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install flask pandas openpyxl ollama
```

یا اگر فایل `requirements.txt` دارید:

```powershell
pip install -r requirements.txt
```

---

## اجرای پروژه (0 → 100)

### 1) کلون/دریافت سورس

پوشه پروژه را آماده کنید (یا فایل‌ها را کپی کنید).

### 2) ساخت پوشه‌ها و فایل پیش‌فرض (اختیاری)

* اگر می‌خواهید بدون انتخاب فایل از UI، رزومه‌ها به‌صورت خودکار خوانده شوند:

  * فایل خود را با نام **`resumes_isaco_fa_10000.xlsx`** داخل پوشه **`uploads/`** قرار دهید.

### 3) اجرای Ollama + مدل

```powershell
ollama serve
ollama pull gemma3:1b
```

> اگر قبلاً کشیده‌اید نیازی به تکرار نیست.

### 4) اجرای Flask

```powershell
(.venv) python app.py
```

خروجی مانند زیر خواهید دید:

```
 * Running on http://127.0.0.1:5000
```

مرورگر را باز کنید: `http://127.0.0.1:5000`

---

## گردش کار (Workflow)

1. **ورود نیازمندی:**

   * یا روی **🎤 گفتار کارفرما** کلیک کنید و فارسی بگویید (مثلاً: «یه تعمیرکار ۲۵ تا ۳۲ سال، سابقه ۳ تا ۶، تهران؛ خدمت فرقی نمی‌کنه»).
   * یا متن را داخل **کادر گفتار/متن** تایپ کنید و روی **✨ پر کردن خودکار فرم** بزنید.
   * سامانه به‌صورت هوشمند:

     * **سن** و **سابقه** را چه **بازه** بدهید (مثلاً `25-32`, `3-6`) و چه **مقدار تکی** (مثلاً `28 سال`، `4 سال سابقه`) → به **بازه مناسب** نگاشت می‌کند.
     * **جنسیت**، **شهر**، **وضعیت خدمت** را استخراج می‌کند.
2. **تکمیل فرم:**

   * در صورت نیاز **کلمات کلیدی ضروری** (مثلاً `PLC, نگهداری پیشگیرانه`) را وارد کنید.
   * **تعداد نفرات برتر** را تعیین کنید (۵/۱۰/۲۰/۵۰).
   * می‌توانید **فایل Excel** را انتخاب کنید؛ در غیر این صورت اگر `uploads/resumes_isaco_fa_10000.xlsx` وجود داشته باشد **خودکار** استفاده می‌شود.
3. **ارسال و پردازش:**

   * کلیک روی **🚀 بارگذاری و شروع تحلیل**.
   * فیلترهای قطعی محلی اجرا می‌شوند (سن/سابقه/شهر/جنسیت/خدمت).
   * اگر **کلمات کلیدی ضروری** داده‌اید، پیش‌فیلتر رزومه‌ها انجام و امتیاز `__kw_hits` محاسبه می‌شود.
   * ردیف‌های باقی‌مانده برای **رتبه‌بندی** به مدل Gemma ارسال می‌شوند (با محدودیت تعداد ردیف جهت کارایی).
   * در صورت خطا یا خروجی غیرقابل‌خواندن LLM، **Fallback** داخلی مشابه‌سازی شباهت متنی اجرا می‌شود.
4. **نتیجه و خروجی:**

   * جدول رتبه‌بندی و **«دلیل انتخاب»** برای هر نفر در UI نمایش داده می‌شود.
   * **دانلود Excel** نتایج با خلاصه تنظیمات بالای شیت.
   * در خلاصه، **نام فایلی که استفاده شده** (آپلودی یا پیش‌فرض) درج می‌شود.

---

## ورودی‌های Excel مورد انتظار

ستون‌ها منعطف هستند؛ سامانه سعی می‌کند آن‌ها را تشخیص دهد و در صورت نبود، مقدار «نامشخص» تولید می‌کند.

* نام: `نام` / `Name`
* جنسیت: `جنسیت` / `Gender`
* نظام وظیفه: `وضعیت نظام وظیفه` / `Military` / `خدمت`
* سن: `سن` / `Age` (اعداد در متن نیز قابل‌خواندن)
* سابقه: `سابقه کار` / `Experience` (اعداد در متن نیز قابل‌خواندن)
* شهر: `شهر` / `City` / `Location`
* توضیحات/مهارت‌ها/عنوان: هر ستونی که شامل `عنوان|title|position|role|مهارت|skill|شرح|description|responsibilit|توضیح|summary` باشد.

> ستون‌های «خلاصه/summary» اضافی قبل از پردازش حذف می‌شوند.

---

## API کوچک برای تست NLP

**تبدیل گفتار/متن → JSON فرم**

```bash
curl -X POST http://127.0.0.1:5000/nlp/parse \
  -H "Content-Type: application/json" \
  -d "{\"utterance\":\"یه تعمیرکار می‌خوام ۲۵ تا ۳۲ سال سنش باشه سابقه کاری ۴ سال، خدمت فرقی نمی‌کنه، مرد باشه\"}"
```

خروجی نمونه:

```json
{
  "job_description": "یه تعمیرکار می‌خوام...",
  "age_range": "25-32",
  "exp_range": "3-6",
  "city": "any",
  "gender_filter": "مرد",
  "military_filter": "همه",
  "must_keywords": [],
  "top_candidates": null
}
```

> توجه: اگر به جای بازه گفتید «۲۸ سال» یا «۴ سال سابقه»، سیستم آن را به **بازه مناسب** نگاشت می‌کند.

---

## نکات اجرایی و اشکال‌زدایی

* **LLM error: CSV parse failed**
  طبیعی است: اگر مدل خروجی CSV خالص ندهد، پارسر تلاش می‌کند اصلاح کند؛ اگر نشد، سیستم به **Fallback** داخلی می‌رود. نتیجه نهایی همچنان نمایش داده می‌شود.
* **مدل Gemma پاسخ نمی‌دهد**
  اطمینان از اجرای **Ollama** و نصب `gemma3:1b`. پورت پیش‌فرض 11434 است.
  تست سریع:

  ```bash
  ollama run gemma3:1b "سلام"
  ```
* **فایل پیش‌فرض پیدا نشد**
  پیام هشدار در UI می‌بینید. یا فایل را به‌صورت دستی انتخاب کنید یا فایل را در `uploads/resumes_isaco_fa_10000.xlsx` قرار دهید.
* **ستون‌ها تشخیص داده نشدند**
  نام ستون‌ها را نزدیک به فارسی/انگلیسی‌های ذکرشده انتخاب کنید تا دقت بیشتر شود.

---

## امنیت و حریم خصوصی

* تمام پردازش روی **سیستم محلی** شما انجام می‌شود (Local-First).
* آپلودها فقط در پوشه **`uploads/`** نگهداری می‌شوند و خروجی‌ها در **`outputs/`**.
* در محیط Production از WSGI (مثلاً **gunicorn/uwsgi**) و **Reverse Proxy** (Nginx) استفاده کنید.

---

## عملکرد و توصیه‌ها

* فایل‌های بسیار بزرگ Excel → زمان پردازش افزایش می‌یابد.
  کد به‌طور خودکار ردیف‌ها را برای ارسال به LLM **کپ** می‌کند (نمونه 300 ردیف) و با **شباهت متنی** نزدیک‌ترین‌ها را انتخاب می‌کند.
* برای بهبود کیفیت رتبه‌بندی:

  * **شرح شغل** را کامل‌تر بنویسید.
  * **کلمات کلیدی ضروری** را دقیق وارد کنید.
  * در صورت نیاز اندازه مدل را تغییر دهید (مثلاً `gemma3:4b`) — نیازمند منابع بیشتر است.

---

## راهنمای استقرار مختصر (Production)

1. ساخت محیط مجازی و نصب وابستگی‌ها
2. اجرای WSGI:

   ```bash
   gunicorn -w 2 -b 0.0.0.0:8000 app:app
   ```
3. قرار دادن Nginx جلوی برنامه + SSL
4. تنظیم دسترسی پوشه‌ها: `uploads/` و `outputs/` قابل نوشتن باشند.

---

## نقشه راه پیشنهادی (Roadmap)

* حساب‌های کاربری + نقش‌ها (ادمین/استخدام‌گر)
* ذخیره سناریوهای فیلتر و نتایج
* تگ‌گذاری سفارشی روی افراد + خروجی Excel با تگ‌ها
* پشتیبانی از چندین فایل/تب‌های Excel
* مدل‌های محلی جایگزین/بزرگ‌تر با سوییچ UI
* اضافه‌کردن ارزیابی خودکار کیفیت خروجی مدل

---

## مجوز

این پروژه برای استفاده داخلی/سازمانی شماست. بنا بر سیاست خود، لایسنس مناسب اضافه کنید (MIT/Apache-2.0/Proprietary).

---

## پشتیبانی سریع

* خطای LLM یا CSV → طبیعی؛ Fallback فعال می‌شود.
* مدل کار نمی‌کند → `ollama serve` و `ollama pull gemma3:1b`.
* فایل پیش‌فرض → در `uploads/` بگذارید یا از UI فایل را انتخاب کنید.

موفق باشید! 🚀
